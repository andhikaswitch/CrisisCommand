.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
node_modules/
dist/
# ...except the built SPA: the ROCm notebook has no Node toolchain, and the
# backend serves this directory so a single tunnel exposes UI + API + WS.
# Rebuild with `npm run build` before committing a release.
!frontend/dist/
.DS_Store
.cache/
.claude/
.pytest_cache/

# Benchmark output IS committed once captured on an AMD GPU: SUBMISSION.md
# points judges at evidence/benchmark.json, so it must exist in a fresh clone.
# The CPU-only file a dev laptop produces is noise — commit the GPU run.
