# Backend runtime. torch is installed separately:
#   local dev (CPU/NVIDIA):  pip install torch
#   MI300X droplet:          ROCm build ships with the vLLM Quick Start image
torch>=2.9
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27

# Dev/test
pytest>=8.0
