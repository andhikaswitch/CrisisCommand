# Backend runtime. torch is installed separately (it is large and platform-
# specific): the Docker image installs the CPU wheel; the MI300X droplet uses
# the ROCm build from the vLLM Quick Start image. Local dev: pip install torch
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27

# Dev/test
pytest>=8.0
